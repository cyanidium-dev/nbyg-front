import Container from "../shared/container/Container";

export default function TagCalculator() {
    return (
        <section>
            <Container>
                <h1 className="sr-only">Tag Calculator</h1>
            </Container>
        </section>
    );
}